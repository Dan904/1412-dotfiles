[user]
	name = Dan904
	email = daniel_linge@yahoo.com
[credential]
	helper = cache --timeout=3600
[core]
	editor = vim
[diff]
	tool = vimdiff
[difftool]
	prompt = false

[color]
        ui = auto

[color "branch"]
        current = yellow bold
        local = green bold
        remote = cyan bold

[color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold
[merge]
	tool = vimdiff

[alias]
        graph = log --oneline --decorate --branches --remotes --tags --graph --all
